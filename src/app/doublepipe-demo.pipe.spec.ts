import { DoublepipeDemoPipe } from './doublepipe-demo.pipe';

describe('DoublepipeDemoPipe', () => {
  it('create an instance', () => {
    const pipe = new DoublepipeDemoPipe();
    expect(pipe).toBeTruthy();
  });
});
